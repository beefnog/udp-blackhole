This is a simple python script for dumping UDP datagrams to stdout. It takes two arguments.

--addr : IPv4 address to bind to, or 0.0.0.0 for any
--port : UDP port to listen on. Anything <= 1024 will require elevated privileges.

